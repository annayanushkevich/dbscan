database = File.open('employees-api/db/seeds.rb')
wordlist = File.open('dbscan/views/scans/wordlist.txt')

datapass = database.readlines
weakpass = wordlist.readlines

datapass.each do |p|
  if(!weakpass.include?(p))
  puts p
  end
end







# <!-- File.read("/employee/seeds.rb").scan /\b#{wordlist.txt}\b/i -->



# <!-- wordlist = ["",""]
# wordlist = Regexp.union(wordlist)

# employees.password.scan(/\b#{wordlist}(?:(?:\s+|\s+)#{wordlist})*\b/) -->